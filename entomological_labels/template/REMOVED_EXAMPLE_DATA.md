# Example data removed from this branch

This is a **work-in-progress** skill, not yet production-ready. The following
example/test files were intentionally **excluded** from this pushed branch
because they contain real specimen data and should not be published:

- `Oxycorynus EMu workbook.xlsx` — test data (EMu export format)
- `labels_with_genomics_filtered.xlsx` — test data (420 specimens)
- `first_template.docx` — original Word mailmerge template (contains real
  specimen records in its preview)
- `Conventions.docx` — reference doc with embedded screenshots and example rows

The full originals are preserved locally on the `entomological-labels-wip`
branch (not pushed). Restore or recreate sanitized equivalents before
finishing the skill.
